this is codebase details
In the “Merge PR” modal and other similar dialogs, the Cancel button is correctly placed on the left side (see SS2).

this is codebase details
In the “Merge PR” modal and other similar dialogs, the Cancel button is correctly placed on the left side (see SS2).

this is codebase details
In the “Merge PR” modal and other similar dialogs, the Cancel button is correctly placed on the left side (see SS2).